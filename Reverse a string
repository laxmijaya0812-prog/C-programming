#include <stdio.h>
#include <string.h>
int main() {
char str[10] = "meeran";
char rev[10];
int i, j = 0,l;
l=strlen(str);
for(i = l - 1; i >= 0; i--) {
rev[j] = str[i];
j++;
}
rev[j] = '\0';
printf("Reversed string: %s", rev);
return 0;
}
