#include <stdio.h>
int factorial
(intn); 

int main(){intn,fact;
scanf("%d",&n);
fact= factorial(n); 
printf("Factorial = %d",fact);
return 0
}
int factorial(int n)
{
int i,f=1;
for(i=1;i<=n;i++)
{
f=f*i;
}
return f;
}
