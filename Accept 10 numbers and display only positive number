#include <stdio.h>

int main() {
    int num, i;

    printf("Enter 10 numbers:\n");

    for(i = 1; i <= 10; i++) {
        scanf("%d", &num);

        if(num > 0) {
            printf("%d ", num);
        }
    }

    return 0;
}
