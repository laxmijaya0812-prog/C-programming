#include <stdio.h>
int main() {
int arr[100], n, target;
printf("Enter number of elements: ");
scanf("%d", &n);
printf("Enter %d elements:\n", n);
for(int i = 0; i < n; i++) {
scanf("%d", &arr[i]);
}
printf("Enter target sum value: ");

12

scanf("%d", &target);
printf("\nUnique quadruplets with sum %d:\n", target);
int found = 0;
for(int i = 0; i < n - 3; i++) {
for(int j = i + 1; j < n - 2; j++) {
for(int k = j + 1; k < n - 1; k++) {
for(int l = k + 1; l < n; l++) {
if(arr[i] + arr[j] + arr[k] + arr[l] == target) {
// Print quadruplet
printf("(%d, %d, %d, %d)\n", arr[i], arr[j], arr[k], arr[l]);
found = 1;
}
}
}
}
}
if(!found) {
printf("No quadruplets found.\n");
}
return 0;
}
