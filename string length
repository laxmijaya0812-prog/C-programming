#include <stdio.h>
int main() {
char str[30]="MOHAMMED FAREETH”; // scanf(“%s”,str)//gets(str)
int i=0,len = 0;
while (str[i] != '\0') {
len++;
i++;
}
printf("Length of the string: %d", len);
return 0;
}
