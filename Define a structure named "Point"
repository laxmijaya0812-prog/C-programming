#include <stdio.h>
#include <math.h>
struct Point {
float x;
float y;
};
int main() {
struct Point p1={2,3};
struct Point p2={4,5};
float distance;
distance = sqrt(pow(p2.x - p1.x, 2) + pow(p2.y - p1.y, 2));
printf("Distance between points = %.2f\n",distance);
return 0;
}
