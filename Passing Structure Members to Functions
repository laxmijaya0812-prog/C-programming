#include <stdio.h>
void display(int r, float m);
struct Student {
int roll;
float marks;
};
int main() {
struct Student s = {10, 89.5};
display(s.roll, s.marks);
return 0;
}
void display(int r, float m) {
printf("Roll: %d\n", r);
printf("Marks: %.2f\n", m);
}
