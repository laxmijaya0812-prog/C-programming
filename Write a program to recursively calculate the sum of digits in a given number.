#include <stdio.h>
int sumOfDigits(int n) ;
int main() {
int num=1234;
int sum= sumOfDigits(num);
printf("Sum of digits = %d",sum);
return 0;
}
int sumOfDigits(int n) {
if (n == 0) {
return 0;
}
return (n % 10) + sumOfDigits(n / 10);
}
