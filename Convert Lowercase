#include <stdio.h>
int main() {
int i = 0;
char str[10]="VENKATSAI";
while (str[i] != '\0') {
if (str[i] >= 'A' && str[i] <= 'Z') {
str[i] = str[i] + 32;
}
i++;
}
printf("Lower Case string: %s", str);
return 0;
}
