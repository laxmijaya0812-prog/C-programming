#include <stdio.h>

int main()
{
    int a[] = {12, 10, 4, 10, 12, 56};
    int n = sizeof(a) / sizeof(a[0]);
    int i, j, flag;

    printf("The array after removing duplicates is:\n");

    for (i = 0; i < n; i++)
    {
        flag = 0;

        for (j = 0; j < i; j++)
        {
            if (a[i] == a[j])
            {
                flag = 1;
                break;
            }
        }

        if (flag == 0)
            printf("%d ", a[i]);
    }

    return 0;
}
