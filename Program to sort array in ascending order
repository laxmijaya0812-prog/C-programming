#include <stdio.h>
void main() {
int arr[10];
int i, j, temp;
printf("Enter elements in array: ");
for(i=0; i<
6; i++)
{

10

scanf("%d", &arr[i]);
}
for(i=0; i<6; i++)
{
for(j=i+1; j<6; j++)
{
if(arr[i] > arr[j])
{
temp = arr[i];
arr[i] = arr[j];
arr[j] = temp;
}
}
}
printf("\nElements of array in ascending order: ");
for(i=0; i<6; i++)
{
printf("%d\t", arr[i]);
}
}
