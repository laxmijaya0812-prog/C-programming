#include <stdio.h>
void main()
{
int a[10] = {1, 2, 3, 4, 5, 6, 7, 8, 9, 0};
int i, largest;
largest = a[0];
for(i = 1; i < 10; i++)
{
if(largest < a[i] )
largest = a[i];
}
printf("Largest element of array is %d", largest);
}
