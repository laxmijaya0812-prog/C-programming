#include <stdio.h>
int stringLength(char str[]);
int main()
{
char str[100];
scanf("%s", str);
int length = stringLength(str);
printf("Length of the string = %d\n", length);
return 0;
}
int stringLength(char str[])
{
int i = 0;
while (str[i] != '\0')
{
i++;
}
return i;
}
