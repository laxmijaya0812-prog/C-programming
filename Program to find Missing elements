#include <stdio.h>
int main() {
int arr[] = {1, 2, 3, 5, 6};
int n = 5;
int total, sum = 0;
total = (6 * (6 + 1)) / 2; // Sum of first n natural numbers
for (int i = 0; i < n; i++) {
sum += arr[i];
}
printf("Missing number: %d\n", total - sum);
return 0;
}
