#include<stdio.h>
int sum(int n);
void main() {
int n=10;
int s;
s = sum(n);
printf("The sum of numbers from 1 to %d is %d", n, s);
}
int sum(int n)
{
int res;
if (n == 1) {
return 1;
} else
{
res = n + sum(n - 1);
}
return res;
}
