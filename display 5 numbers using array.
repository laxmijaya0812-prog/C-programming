#include <stdio.h>
void main()
{
int i,a[5];
printf("Enter 5 integers: ");
for(i = 0; i < 5; ++i)
{
scanf("%d", &a[i]);
}
printf("Displaying integers: ");
for(int i = 0; i < 5; ++i)
{
2 3 4 5 6

4

printf("%d
\n",
a[i]);

}
}
